<div class="container mt-4" style="color: white;">
  <%= simple_form_for @list do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Submit list", class: "btn btn-outline-light" %>
  <% end %>
</div>
